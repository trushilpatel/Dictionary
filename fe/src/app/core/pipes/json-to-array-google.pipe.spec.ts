import { JsonToArrayGooglePipe } from './json-to-array-google.pipe';

describe('JsonToArrayGooglePipe', () => {
  it('create an instance', () => {
    const pipe = new JsonToArrayGooglePipe();
    expect(pipe).toBeTruthy();
  });
});
